<?xml version="1.0"?>
<!DOCTYPE fontconfig SYSTEM "fonts.dtd">
<fontconfig>
  <!-- Tell fontconfig to search for fonts in the app root directory -->
  <dir>/app</dir>
  <!-- Write font cache to a writeable directory in Heroku -->
  <cachedir>/tmp/fontconfig</cachedir>
</fontconfig>
